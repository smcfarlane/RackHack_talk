---
name: rack_middleware_continue
position: 10
type: list-slide
sections:
  title: Rack Middleware Cont.
  bullet_list:
    - >
      When you call <pre>@app.call(env)</pre> the rest of the middleware
      and the app will run
    - >
      You can add values to `env` to pass it on to following
      middleware and the app
    - >
      You can get the result of <pre>@app.call(env)</pre> and change it
      or add to it
